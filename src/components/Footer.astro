---
// Footer Component for Rodax Refacciones
// Comprehensive footer with contact, links, and company info

// Footer navigation links
const footerLinks = {
  productos: [
    { name: "Filtros", href: "/categoria/filtros" },
    { name: "Frenos", href: "/categoria/frenos" },
    { name: "Motor", href: "/categoria/motor" },
    { name: "Transmisión", href: "/categoria/transmision" },
    { name: "Eléctrico", href: "/categoria/electrico" },
    { name: "Ver Todos", href: "/productos" }
  ],
  servicios: [
    { name: "Asesoría Técnica", href: "/asesoria" },
    { name: "Entrega Express", href: "/entrega" },
    { name: "Instalación", href: "/instalacion" },
    { name: "Garantías", href: "/garantias" },
    { name: "Catálogo Digital", href: "/catalogo" }
  ],
  empresa: [
    { name: "Quiénes Somos", href: "/about" },
    { name: "Certificaciones", href: "/certificaciones" },
    { name: "Trabaja con Nosotros", href: "/empleos" },
    { name: "Noticias", href: "/blog" },
    { name: "Contacto", href: "/contacto" }
  ],
  soporte: [
    { name: "Centro de Ayuda", href: "/ayuda" },
    { name: "Preguntas Frecuentes", href: "/faq" },
    { name: "Políticas", href: "/politicas" },
    { name: "Términos y Condiciones", href: "/terminos" },
    { name: "Privacidad", href: "/privacidad" }
  ]
};

// Trusted brands for footer
const trustedBrands = [
  "Fleetguard", "Donaldson", "Bendix", "Shell", "AMTRK", "Motorcraft", "Philips", "Bosch"
];

// Contact information
const contactInfo = {
  address: "Av. Industria Automotriz 1234, Zona Industrial, 25000 Saltillo, Coahuila",
  phone: "+52 (844) 123-4567",
  whatsapp: "+52 (844) 987-6543",
  email: "ventas@rodaxrefacciones.com",
  hours: "Lun-Vie: 8:00-18:00, Sáb: 9:00-14:00"
};
---

<!-- Footer Section -->
<footer class="bg-rodax-dark text-white">
  
  <!-- Main Footer Content -->
  <div class="py-12">
    <div class="container mx-auto px-4">
      <div class="grid lg:grid-cols-5 gap-8">
        
        <!-- Company Info -->
        <div class="lg:col-span-2">
          <!-- Logo and Tagline -->
          <div class="flex items-center space-x-3 mb-6">
            <div class="w-12 h-12 bg-rodax-orange rounded-lg flex items-center justify-center">
              <span class="text-xl font-bold">R</span>
            </div>
            <div>
              <h3 class="text-2xl font-bold text-rodax-orange">RODAX</h3>
              <p class="text-sm text-gray-300">Refacciones</p>
            </div>
          </div>
          
          <p class="text-gray-300 mb-6 leading-relaxed">
            Tu socio de confianza en Saltillo para refacciones de transporte pesado. 
            Más de 15 años brindando calidad, servicio y confiabilidad a la industria del autotransporte.
          </p>

          <!-- Contact Information -->
          <div class="space-y-3 mb-6">
            <div class="flex items-start space-x-3">
              <span class="text-rodax-orange mt-1">📍</span>
              <div>
                <p class="text-sm font-medium">Dirección:</p>
                <p class="text-sm text-gray-300">{contactInfo.address}</p>
              </div>
            </div>
            <div class="flex items-center space-x-3">
              <span class="text-rodax-orange">📞</span>
              <div>
                <p class="text-sm font-medium">Teléfono:</p>
                <a href={`tel:${contactInfo.phone}`} class="text-sm text-gray-300 hover:text-rodax-orange">
                  {contactInfo.phone}
                </a>
              </div>
            </div>
            <div class="flex items-center space-x-3">
              <span class="text-rodax-orange">💬</span>
              <div>
                <p class="text-sm font-medium">WhatsApp:</p>
                <a href={`https://wa.me/${contactInfo.whatsapp.replace(/\D/g, '')}`} class="text-sm text-gray-300 hover:text-rodax-orange">
                  {contactInfo.whatsapp}
                </a>
              </div>
            </div>
            <div class="flex items-center space-x-3">
              <span class="text-rodax-orange">✉️</span>
              <div>
                <p class="text-sm font-medium">Email:</p>
                <a href={`mailto:${contactInfo.email}`} class="text-sm text-gray-300 hover:text-rodax-orange">
                  {contactInfo.email}
                </a>
              </div>
            </div>
            <div class="flex items-center space-x-3">
              <span class="text-rodax-orange">🕒</span>
              <div>
                <p class="text-sm font-medium">Horarios:</p>
                <p class="text-sm text-gray-300">{contactInfo.hours}</p>
              </div>
            </div>
          </div>

          <!-- Social Media -->
          <div class="flex space-x-4">
            <a href="#" class="bg-blue-600 w-10 h-10 rounded-full flex items-center justify-center hover:bg-blue-700 transition-colors">
              <span class="text-sm">📘</span>
            </a>
            <a href="#" class="bg-pink-600 w-10 h-10 rounded-full flex items-center justify-center hover:bg-pink-700 transition-colors">
              <span class="text-sm">📷</span>
            </a>
            <a href="#" class="bg-red-600 w-10 h-10 rounded-full flex items-center justify-center hover:bg-red-700 transition-colors">
              <span class="text-sm">📺</span>
            </a>
            <a href="#" class="bg-blue-400 w-10 h-10 rounded-full flex items-center justify-center hover:bg-blue-500 transition-colors">
              <span class="text-sm">🐦</span>
            </a>
          </div>
        </div>

        <!-- Products Links -->
        <div>
          <h4 class="font-bold text-lg mb-4">Productos</h4>
          <ul class="space-y-2">
            {footerLinks.productos.map((link) => (
              <li>
                <a href={link.href} class="text-gray-300 hover:text-rodax-orange transition-colors text-sm">
                  {link.name}
                </a>
              </li>
            ))}
          </ul>
        </div>

        <!-- Services Links -->
        <div>
          <h4 class="font-bold text-lg mb-4">Servicios</h4>
          <ul class="space-y-2">
            {footerLinks.servicios.map((link) => (
              <li>
                <a href={link.href} class="text-gray-300 hover:text-rodax-orange transition-colors text-sm">
                  {link.name}
                </a>
              </li>
            ))}
          </ul>
        </div>

        <!-- Company & Support -->
        <div>
          <h4 class="font-bold text-lg mb-4">Empresa</h4>
          <ul class="space-y-2 mb-6">
            {footerLinks.empresa.map((link) => (
              <li>
                <a href={link.href} class="text-gray-300 hover:text-rodax-orange transition-colors text-sm">
                  {link.name}
                </a>
              </li>
            ))}
          </ul>
          
          <h4 class="font-bold text-lg mb-4">Soporte</h4>
          <ul class="space-y-2">
            {footerLinks.soporte.slice(0, 3).map((link) => (
              <li>
                <a href={link.href} class="text-gray-300 hover:text-rodax-orange transition-colors text-sm">
                  {link.name}
                </a>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </div>
  </div>

  <!-- Trusted Brands Section -->
  <div class="border-t border-gray-700 py-8">
    <div class="container mx-auto px-4">
      <h4 class="text-center font-semibold text-gray-300 mb-6">Marcas de Confianza</h4>
      <div class="flex flex-wrap justify-center items-center gap-6">
        {trustedBrands.map((brand) => (
          <div class="text-gray-400 hover:text-white transition-colors text-sm font-medium px-3 py-1 rounded border border-gray-600 hover:border-rodax-orange">
            {brand}
          </div>
        ))}
      </div>
    </div>
  </div>

  <!-- Newsletter Section -->
  <div class="border-t border-gray-700 py-8">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center">
        <h4 class="text-xl font-bold mb-4">📧 Mantente Informado</h4>
        <p class="text-gray-300 mb-6">
          Recibe las últimas ofertas, nuevos productos y consejos técnicos directamente en tu email
        </p>
        <div class="flex flex-col sm:flex-row gap-4 max-w-md mx-auto">
          <input 
            type="email" 
            placeholder="Tu email aquí..."
            class="flex-1 px-4 py-3 bg-gray-800 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-rodax-orange"
          />
          <button class="bg-rodax-orange hover:bg-orange-600 text-white px-6 py-3 rounded-lg font-medium transition-colors">
            Suscribirse
          </button>
        </div>
        <p class="text-xs text-gray-400 mt-3">
          No compartimos tu información. Puedes darte de baja en cualquier momento.
        </p>
      </div>
    </div>
  </div>

  <!-- Copyright Bar -->
  <div class="border-t border-gray-700 py-6">
    <div class="container mx-auto px-4">
      <div class="flex flex-col md:flex-row justify-between items-center">
        <div class="text-sm text-gray-400 mb-4 md:mb-0">
          © 2024 Rodax Refacciones. Todos los derechos reservados. | RFC: RRF080123ABC
        </div>
        <div class="flex space-x-6 text-xs text-gray-400">
          <a href="/terminos" class="hover:text-rodax-orange transition-colors">Términos</a>
          <a href="/privacidad" class="hover:text-rodax-orange transition-colors">Privacidad</a>
          <a href="/cookies" class="hover:text-rodax-orange transition-colors">Cookies</a>
          <a href="/sitemap" class="hover:text-rodax-orange transition-colors">Mapa del Sitio</a>
        </div>
      </div>
    </div>
  </div>

  <!-- Quick Access Floating Button -->
  <div class="fixed bottom-6 right-6 z-50">
    <div class="flex flex-col space-y-3">
      <!-- WhatsApp Button -->
      <a 
        href={`https://wa.me/${contactInfo.whatsapp.replace(/\D/g, '')}`}
        class="bg-green-500 hover:bg-green-600 text-white w-14 h-14 rounded-full flex items-center justify-center shadow-lg hover:shadow-xl transition-all duration-300 group"
        title="Contactar por WhatsApp"
      >
        <span class="text-2xl">💬</span>
      </a>
      
      <!-- Phone Button -->
      <a 
        href={`tel:${contactInfo.phone}`}
        class="bg-rodax-orange hover:bg-orange-600 text-white w-14 h-14 rounded-full flex items-center justify-center shadow-lg hover:shadow-xl transition-all duration-300"
        title="Llamar ahora"
      >
        <span class="text-2xl">📞</span>
      </a>
    </div>
  </div>
</footer>

<style>
  /* Smooth hover animations */
  a {
    transition: all 0.3s ease;
  }

  /* Custom scrollbar for any overflow content */
  *::-webkit-scrollbar {
    width: 6px;
    height: 6px;
  }

  *::-webkit-scrollbar-track {
    background: #374151;
  }

  *::-webkit-scrollbar-thumb {
    background: #ea580c;
    border-radius: 3px;
  }
</style> 